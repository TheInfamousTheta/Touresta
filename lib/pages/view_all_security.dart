import 'package:flutter/material.dart';

class ViewAllSecurity extends StatelessWidget {
  const ViewAllSecurity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View All Security Page'),),
    );
  }
}
