import 'package:flutter/material.dart';

class ViewAllFinance extends StatelessWidget {
  const ViewAllFinance({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View All Finance Page'),),
    );
  }
}
