import 'package:flutter/material.dart';

class ViewAllIoT extends StatelessWidget {
  const ViewAllIoT({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View All IoT Page'),),
    );
  }
}
